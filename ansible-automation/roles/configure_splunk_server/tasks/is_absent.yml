---
# tasks file for configure_splunk_server — absent
#
# Removes the extracted directory tree.

- name: Remove extracted destination
  ansible.builtin.file:
    path: "{{ extract_dest | default(install_dir) }}"
    state: absent
  when: (extract_dest | default(install_dir | default(''))) | length > 0
