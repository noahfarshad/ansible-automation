---
# tasks file for download_microservice_dependencies — absent
#
# This role's removal logic is not auto-derivable from is_present.yml.
# Default behavior: no-op with explanatory debug message.

- name: "download_microservice_dependencies — absent is a documented no-op"
  ansible.builtin.debug:
    msg: >-
      The 'absent' state for role 'download_microservice_dependencies' is intentionally not implemented.
      Edit tasks/is_absent.yml if a teardown is needed for your environment.
