---
# tasks file for upgrade_vmware_tools — absent
#
# This role uses third-party collection modules (cisco/dellemc/community/etc.).
# Reversing those operations requires using the collection's own teardown
# patterns, which vary by collection.
#
# Default behavior: no-op with explanatory debug message.

- name: "upgrade_vmware_tools — absent is a documented no-op"
  ansible.builtin.debug:
    msg: >-
      The 'absent' state for role 'upgrade_vmware_tools' is intentionally not implemented.
      Reversing collection-module operations requires per-collection knowledge.
