---
# tasks file for deployMicroservices — absent
#
# This role only performs checks/assertions (debug, fail, assert).
# It makes no changes to the system, so 'absent' has no meaningful action.

- name: "deployMicroservices — absent is a no-op (this role only performs checks)"
  ansible.builtin.debug:
    msg: "Role 'deployMicroservices' makes no system changes; nothing to undo."
