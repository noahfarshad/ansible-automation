---
# tasks file for gather_facts — absent
#
# This role only performs checks/assertions (debug, fail, assert).
# It makes no changes to the system, so 'absent' has no meaningful action.

- name: "gather_facts — absent is a no-op (this role only performs checks)"
  ansible.builtin.debug:
    msg: "Role 'gather_facts' makes no system changes; nothing to undo."
