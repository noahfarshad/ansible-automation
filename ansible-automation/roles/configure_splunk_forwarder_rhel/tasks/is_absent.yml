---
# tasks file for configure_splunk_forwarder_rhel — absent
#
# Stops and disables the service. Does NOT uninstall the package.

- name: Stop and disable service
  ansible.builtin.service:
    name: "SplunkForwarder"
    state: stopped
    enabled: false
  failed_when: false
