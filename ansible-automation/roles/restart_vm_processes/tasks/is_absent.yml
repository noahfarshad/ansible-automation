---
# tasks file for restart_vm_processes — absent
#
# This role only performs checks/assertions (debug, fail, assert).
# It makes no changes to the system, so 'absent' has no meaningful action.

- name: "restart_vm_processes — absent is a no-op (this role only performs checks)"
  ansible.builtin.debug:
    msg: "Role 'restart_vm_processes' makes no system changes; nothing to undo."
