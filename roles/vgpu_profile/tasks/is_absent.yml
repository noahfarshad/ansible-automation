---
# tasks file for vgpu_profile — absent
#
# This role only performs checks/assertions (debug, fail, assert).
# It makes no changes to the system, so 'absent' has no meaningful action.

- name: "vgpu_profile — absent is a no-op (this role only performs checks)"
  ansible.builtin.debug:
    msg: "Role 'vgpu_profile' makes no system changes; nothing to undo."
