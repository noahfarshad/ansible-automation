---
# tasks file for configure_rhel7 — absent
#
# Stops and disables the service. Does NOT uninstall the package.

- name: Stop and disable service
  ansible.builtin.service:
    name: "network"
    state: stopped
    enabled: false
  failed_when: false
