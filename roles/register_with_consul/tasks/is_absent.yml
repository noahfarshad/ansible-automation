---
# tasks file for register_with_consul — absent
#
# This role only performs checks/assertions (debug, fail, assert).
# It makes no changes to the system, so 'absent' has no meaningful action.

- name: "register_with_consul — absent is a no-op (this role only performs checks)"
  ansible.builtin.debug:
    msg: "Role 'register_with_consul' makes no system changes; nothing to undo."
