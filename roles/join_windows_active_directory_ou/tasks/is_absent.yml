---
# tasks file for join_windows_active_directory_ou — absent
#
# This role's removal logic is not auto-derivable from is_present.yml.
# Default behavior: no-op with explanatory debug message.

- name: "join_windows_active_directory_ou — absent is a documented no-op"
  ansible.builtin.debug:
    msg: >-
      The 'absent' state for role 'join_windows_active_directory_ou' is intentionally not implemented.
      Edit tasks/is_absent.yml if a teardown is needed for your environment.
