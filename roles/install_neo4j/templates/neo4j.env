# Source file used to set the environmental variables for Neo4j
# This is used because there are multiple versions of java installed on the VM where Neo4j will be running
export JAVA_CMD="{{java_install_directory}}/bin/java"