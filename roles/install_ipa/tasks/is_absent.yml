---
# tasks file for install_ipa — absent
#
# Uninstalls FreeIPA / Identity Management Server.

- name: Uninstall IPA server
  ansible.builtin.command: ipa-server-install --uninstall --unattended
  args:
    removes: /etc/ipa/default.conf
