---
# tasks file for restart_linux_service_java_app_with_port — absent
#
# Stops and disables the service. Does NOT uninstall the package.

- name: Stop and disable service
  ansible.builtin.service:
    name: "{{service_name}}"
    state: stopped
    enabled: false
  failed_when: false
