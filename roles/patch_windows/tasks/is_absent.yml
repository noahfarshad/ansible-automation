---
# tasks file for patch_windows — absent

- name: Stop and disable Windows service
  ansible.windows.win_service:
    name: "{{ service_name }}"
    state: stopped
    start_mode: disabled
  failed_when: false
  when: service_name is defined
