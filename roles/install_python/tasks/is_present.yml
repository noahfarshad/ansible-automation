---
# tasks file for install_python
# - name: Uninstall Python
#   ansible.windows.win_package:
#     path: "{{python_install_loc}}{{pythonOLD}}"
#     arguments: /qn /norestart
#     state: absent
#   become: true
#   become_method: runas
#   become_flags: logon_type=new_credentials logon_flags=netcredentials_only
#   vars:
#     ansible_become_user: "{{system_name}}\\{{domainadmin_user}}"
#     ansible_become_password: '{{domain_password}}'
#   ignore_errors: yes

- name: Install python from a network share specifying the credentials
  ansible.windows.win_package:
    path: "{{artifact_staging}}{{python}}"
    arguments: /quiet
    state: present
  become: true
  become_method: runas
  become_flags: logon_type=new_credentials logon_flags=netcredentials_only
  vars:
    ansible_become_user: "{{system_name}}\\{{domainadmin_user}}"
    ansible_become_password: '{{domain_password}}'