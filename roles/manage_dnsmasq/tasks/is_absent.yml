---
# tasks file for manage_dnsmasq — absent
#
# This role only performs checks/assertions (debug, fail, assert).
# It makes no changes to the system, so 'absent' has no meaningful action.

- name: "manage_dnsmasq — absent is a no-op (this role only performs checks)"
  ansible.builtin.debug:
    msg: "Role 'manage_dnsmasq' makes no system changes; nothing to undo."
