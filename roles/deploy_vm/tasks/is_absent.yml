---
# tasks file for deploy_vm — absent
#
# This role only performs checks/assertions (debug, fail, assert).
# It makes no changes to the system, so 'absent' has no meaningful action.

- name: "deploy_vm — absent is a no-op (this role only performs checks)"
  ansible.builtin.debug:
    msg: "Role 'deploy_vm' makes no system changes; nothing to undo."
